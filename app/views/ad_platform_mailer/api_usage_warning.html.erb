<h1>Google Ads API Usage Alert</h1>

<p>The Google Ads API has used <strong><%= number_with_delimiter(@operations_today) %></strong> of <strong><%= number_with_delimiter(@limit) %></strong> daily operations (<%= @percentage %>%).</p>

<p>Remaining today: <strong><%= number_with_delimiter(@remaining) %></strong> operations.</p>

<p>
  mbuzz has Basic Access to the Google Ads API, which allows 15,000 operations per day.
  If usage regularly approaches this limit, apply for
  <a href="https://developers.google.com/google-ads/api/docs/access-levels">Standard Access</a>.
</p>

<p>
  Thanks,<br>
  The mbuzz Team
</p>
