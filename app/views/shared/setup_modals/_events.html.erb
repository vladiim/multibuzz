<div id="setup-events-modal" class="hidden fixed inset-0 z-50" data-controller="modal">
  <div class="fixed inset-0 bg-black bg-opacity-25" data-action="click->modal#close"></div>
  <div class="fixed inset-0 flex items-center justify-center p-4">
    <div class="bg-white rounded-lg shadow-xl max-w-2xl w-full max-h-[90vh] overflow-y-auto">
      <div class="px-6 py-4 border-b border-gray-200 flex items-center justify-between sticky top-0 bg-white">
        <h3 class="text-lg font-medium text-gray-900">Track your first event</h3>
        <button type="button" data-action="click->modal#close" class="text-gray-400 hover:text-gray-500">
          <svg class="w-5 h-5" fill="none" stroke="currentColor" viewBox="0 0 24 24">
            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M6 18L18 6M6 6l12 12" />
          </svg>
        </button>
      </div>
      <div class="px-6 py-4 space-y-4">
        <p class="text-sm text-gray-600">
          Send events to mbuzz to track user actions on your site. Events are the foundation of attribution - they capture
          what users do before they convert.
        </p>

        <div class="bg-gray-50 rounded-lg p-4">
          <h4 class="text-sm font-medium text-gray-900 mb-3">Example: Track a page view</h4>
          <%= track_event_example %>
        </div>

        <div class="bg-blue-50 border border-blue-200 rounded-lg p-4">
          <h4 class="text-sm font-medium text-blue-900 mb-1">Common events to track</h4>
          <ul class="text-sm text-blue-700 list-disc list-inside space-y-1">
            <li>Page views (automatically captured by SDK)</li>
            <li>Button clicks and form submissions</li>
            <li>Feature usage and engagement</li>
            <li>Errors and exceptions</li>
          </ul>
        </div>
      </div>
      <div class="px-6 py-4 border-t border-gray-200 flex justify-between items-center">
        <%= link_to "View full documentation", docs_path(page: "getting-started"),
          class: "text-sm text-indigo-600 hover:text-indigo-900" %>
        <button type="button" data-action="click->modal#close"
          class="px-4 py-2 text-sm font-medium text-white bg-indigo-600 hover:bg-indigo-700 rounded-md">
          Got it
        </button>
      </div>
    </div>
  </div>
</div>
