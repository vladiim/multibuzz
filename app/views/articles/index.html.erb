<% content_for(:title) { "MTA Academy - Multi-Touch Attribution Guides" } %>
<% content_for(:meta_description) { "Master multi-touch attribution with technical guides, implementation patterns, and real-world examples. Learn about attribution models, server-side tracking, and marketing measurement." } %>
<% content_for(:og_title) { "MTA Academy | mbuzz" } %>
<% content_for(:canonical_url) { academy_url } %>

<section class="py-16 px-6">
  <div class="max-w-5xl mx-auto">
    <h1 class="text-4xl font-bold text-gray-900 tracking-tight mb-4">
      MTA Academy
    </h1>
    <p class="text-xl text-gray-600 max-w-2xl">
      Master multi-touch attribution with technical guides, implementation patterns, and real-world examples.
    </p>
  </div>
</section>

<section class="py-12 px-6">
  <div class="max-w-5xl mx-auto">
    <% Article::SECTIONS.each do |section| %>
      <% articles = @sections[section] %>
      <% next unless articles&.any? %>
      <div class="mb-12">
        <h2 class="text-xl font-bold text-gray-900 mb-4 capitalize">
          <%= section.tr("-", " ") %>
        </h2>
        <div class="grid grid-cols-1 md:grid-cols-2 gap-4">
          <% articles.each do |article| %>
            <%= render "article_card", article: article %>
          <% end %>
        </div>
      </div>
    <% end %>
  </div>
</section>
