<%= turbo_frame_tag "demo_spend" do %>
  <div class="dashboard-section">
    <!-- Demo connection status -->
    <div class="flex items-center gap-3 mb-6">
      <div class="flex items-center gap-1.5">
        <span class="w-2 h-2 bg-green-500 rounded-full"></span>
        <span class="text-sm text-gray-600">Google Ads connected</span>
      </div>
      <span class="text-xs text-gray-400">Last sync: 12 minutes ago</span>
    </div>

    <!-- Hero KPI Cards -->
    <%= render "dashboard/spend/hero_metrics", totals: @data[:totals] %>

    <!-- Trend Chart + Channel Summary -->
    <div class="grid grid-cols-1 lg:grid-cols-5 gap-6 mb-6">
      <%= render "dashboard/spend/trend_chart", time_series: @data[:time_series] %>
      <%= render "dashboard/spend/channel_summary", by_channel: @data[:by_channel] %>
    </div>

    <!-- Deep Analysis Tabs -->
    <div class="bg-white rounded-lg shadow">
      <div class="border-b border-gray-200 px-4 sm:px-6"
        data-controller="toggle"
        data-toggle-default-value="overview"
        data-toggle-active-class="border-indigo-500 text-indigo-600"
        data-toggle-inactive-class="border-transparent text-gray-500 hover:text-gray-700 hover:border-gray-300">

        <nav class="-mb-px flex gap-4 sm:gap-6 overflow-x-auto">
          <button type="button" data-toggle-target="trigger" data-action="toggle#select" data-value="overview"
            class="py-3 px-1 text-sm font-medium border-b-2 whitespace-nowrap">Overview</button>
          <button type="button" data-toggle-target="trigger" data-action="toggle#select" data-value="hourly_device"
            class="py-3 px-1 text-sm font-medium border-b-2 whitespace-nowrap">Hourly / Device</button>
          <button type="button" data-toggle-target="trigger" data-action="toggle#select" data-value="payback"
            class="py-3 px-1 text-sm font-medium border-b-2 whitespace-nowrap">Payback Period</button>
          <button type="button" data-toggle-target="trigger" data-action="toggle#select" data-value="recommendations"
            class="py-3 px-1 text-sm font-medium border-b-2 whitespace-nowrap">Recommendations</button>
        </nav>

        <!-- Overview Sub-Tab -->
        <div data-toggle-target="content" data-value="overview">
          <div class="py-4 sm:py-6">
            <%= render "dashboard/spend/channel_table", by_channel: @data[:by_channel], totals: @data[:totals] %>
          </div>
        </div>

        <!-- Hourly / Device Sub-Tab -->
        <div data-toggle-target="content" data-value="hourly_device" class="hidden">
          <div class="py-4 sm:py-6">
            <%= render "dashboard/spend/hourly_device", by_hour: @data[:by_hour], by_device: @data[:by_device] %>
          </div>
        </div>

        <!-- Payback Period Sub-Tab -->
        <div data-toggle-target="content" data-value="payback" class="hidden">
          <div class="py-4 sm:py-6">
            <%= render "dashboard/spend/payback_period", payback: @data[:payback], totals: @data[:totals] %>
          </div>
        </div>

        <!-- Recommendations Sub-Tab -->
        <div data-toggle-target="content" data-value="recommendations" class="hidden">
          <div class="py-4 sm:py-6">
            <%= render "dashboard/spend/recommendations", recommendations: @data[:recommendations] %>
          </div>
        </div>
      </div>
    </div>
  </div>
<% end %>
