# frozen_string_literal: true

module App
  # --- Domain ---
  DOMAIN = "mbuzz.co"
  HOST = DOMAIN
  BASE_URL = "https://#{DOMAIN}"

  # --- Publisher Info (for schema.org) ---
  PUBLISHER_NAME = "mbuzz"
  PUBLISHER_URL = BASE_URL

  # --- Contact ---
  EMAIL = "hello@#{DOMAIN}"
end
