# frozen_string_literal: true

# Outbound conversion-feedback adapters. Mirror of `AdPlatforms::*`
# (inbound: OAuth + spend pull). Each subnamespace owns one platform's
# wire client + payload builder + dispatcher.
module AdDestinations
end
